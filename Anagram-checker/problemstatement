Here is the link to problem statement:
https://www.hackerrank.com/contests/crescent-practice-1st-and-2nd-year/challenges/anagram-3

Below is the problem statement:

An anagram is a type of word play, the result of rearranging the letters of a word or phrase to produce a new word or phrase, using all the original letters exactly once; for example torchwood can be rearranged into doctorwho.

Given two strings s1 and s2,find if they are anagrams of each other.

Strings contain lowercase(a-z) characters,uppercase characters(A-Z) and digits(0-9).

Input Format

Two strings will be given as input.

Output Format

Print "Anagram" if they are anagrams of each other,else print "Not Anagram".

Sample Input

abc bca

Sample Output

Anagram

Explanation

You can see that they are anagrams of each other as when you rearrange "bca" you can get "abc". Similarly,if you rearrange "abc",you can get "bca".
